name: CI

on:
  pull_request:
  # No push trigger for main: deploy.yml calls this workflow as its gating
  # job on every main push, so a standalone run there would be a duplicate.
  # Lets deploy.yml run this exact validation before deploying, so a deploy
  # can never start from a commit that hasn't passed CI.
  workflow_call:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate:
    name: Validate template & JS
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Set up Python (for SAM CLI)
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Set up AWS SAM CLI
        uses: aws-actions/setup-sam@v3
        with:
          use-installer: true

      - name: Lint SAM template
        # No AWS creds needed: --lint runs cfn-lint locally against the template.
        run: sam validate --lint --region us-east-1

      - name: Check JavaScript syntax
        # App scripts live in js/ (core, content, packs, app) plus the
        # standalone files (worker, Node reference, service worker) and the
        # backend Lambda handlers. Parse them all so a typo can't ship a
        # broken page, worker, cache, or API route.
        run: |
          node --check worker.js
          node --check workers-atomics.js
          node --check sw.js
          for f in js/*.js js/packs/*.js backend/functions/*/*.mjs; do
            [ -e "$f" ] || continue
            node --check "$f"
          done
          echo "all app scripts parse OK"

      - name: Validate content
        # Executes every write-it exercise's reference solution against its
        # own sandbox tests and runs every drill demo — a broken exercise or
        # a content pack with failing assertions cannot merge.
        run: node tools/validate-content.mjs

      - name: Run practice pack tests against reference solutions
        # Stages practice/solutions/*.mjs over the skeleton slots and runs
        # every practice/*.test.mjs suite, so an edit that breaks a test or
        # its reference solution (or orphans either one) cannot merge.
        run: node tools/test-solutions.mjs

      - name: Check service worker precaches every app script
        # A js/ file missing from the SHELL list breaks the app offline.
        run: |
          node -e '
            const fs = require("fs");
            const sw = fs.readFileSync("sw.js", "utf8");
            const files = ["js/core.js", "js/content.js", "js/app.js",
              ...(fs.existsSync("js/packs") ? fs.readdirSync("js/packs").filter(f => f.endsWith(".js")).map(f => "js/packs/" + f) : [])];
            const missing = files.filter(f => !sw.includes("./" + f));
            if (missing.length) { console.error("sw.js SHELL is missing: " + missing.join(", ")); process.exit(1); }
            console.log("sw.js precaches all " + files.length + " app scripts");
          '
